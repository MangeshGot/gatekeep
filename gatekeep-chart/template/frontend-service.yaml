apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.frontend.name}}-service
spec:
  type: {{ .Values.frontend.service_type}}
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  selector:
    app: {{ .Values.frontend.name}}