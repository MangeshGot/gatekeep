apiVersion: external-secrets.io/v1
kind: SecretStore
metadata:
  name: aws-secret-manager-store
  namespace: external-secrets
spec:
  provider:
    aws:
      service: SecretsManager
      region: us-east-1
      # Note: No 'auth:' block needed when using IRSA!
